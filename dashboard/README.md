# Dashboard

Folder này chứa các file CSV thống kê do pipeline xuất ra để Power BI đọc trực tiếp:

- `quality_summary.csv`
- `storage_benchmark.csv`
- `query_benchmark.csv`

Các file CSV là output runtime, không phải source code.
